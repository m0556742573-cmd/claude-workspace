# כללי תיעוד — כל הפרויקטים תחת workspace זה

כל תת-פרויקט תחת `myProjekts/<שם>/` מנהל תיקיית `docs/` עם המבנה הבא. עדכן את הקבצים הרלוונטיים **מיד** אחרי כל פעולה משמעותית (שלב הושלם, החלטה התקבלה, שינוי תשתית) — לא לחכות לתזכורת, ולא לעדכן על כל פעולה טריוויאלית (רעש).

- **`docs/INDEX.md`** — הקובץ הראשון לפתוח. שורה אחת לכל קובץ אחר בתיקייה (מה יש בו), כולל רשימה מתעדכנת של כל קבצי `decisions/`. **חובה לעדכן את זה בכל פעם שנוסף/משתנה קובץ אחר** — אחרת האינדקס משקר.
- `docs/overview.md` — למה הפרויקט קיים, מטרות-על. כמעט לא משתנה.
- `docs/architecture.md` — איך זה בנוי כרגע. מסמך חי, נערך במקום.
- `docs/status.md` — איפה אנחנו עכשיו, מה הצעד הבא. הכי מתעדכן.
- `docs/decisions/NNNN-כותרת.md` — ADR לכל החלטה ארכיטקטונית משמעותית (Status/Context/Decision/Consequences). קובץ חדש לכל החלטה, לא עריכה של ישן. ADR רק להחלטות ששווה שמישהו ישאל עליהן "למה" בעוד חצי שנה — לא לכל פרט טכני.
- `docs/log.md` — יומן פעולות כרונולוגי, תמציתי (שורה-שתיים), append-only. להפנות לקומיט גיט במקום לשכפל תוכן שכבר שם.
- `docs/access.md` — מרשם הרשאות/גישות (מפתחות, טוקנים, מי יצר ומתי, מה זה נותן גישה אליו) — **לא הסודות עצמם**, רק לאן הם מצביעים. **local-only, לא נכנס לגיט.**

ראה [PROJECTS.md](PROJECTS.md) לאינדקס כל הפרויקטים ב-workspace.
