# אינדקס — פרויקט גרינוואלד

- [`overview.md`](overview.md) — כוכב צפוני, תיחום scope, עשר מטרות-העל עם יעדים מדידים, מודל 33 הישויות (תמצית), מקורות המידע.
- [`architecture.md`](architecture.md) — איך זה בנוי כרגע: Supabase self-hosted, Traefik/DNS, אבטחה, גיבויים. מסמך חי.
- [`status.md`](status.md) — מה הושלם, מה פתוח, מה הצעד הבא. הכי מתעדכן — פותחים ראשון.
- [`log.md`](log.md) — יומן פעולות כרונולוגי.
- `access.md` — מרשם הרשאות/גישות. **local-only, לא בגיט — ולכן מוזכר כאן בשם ולא כקישור: בכל שכפול של הריפו הקובץ אינו קיים.**
- `entities/` — **כל טבלת SQL בקובץ נפרד משלה**. **ר' [`entities/INDEX.md`](entities/INDEX.md) לרשימה המלאה** (טבלה עם תרגום + הסבר + סטטוס לכל ישות) — לא משוכפל כאן, כדי לא לתחזק שתי רשימות זהות.
- `source-materials/` — עותקים פיזיים של מסמכי המקור (לא רק הפניה חיצונית):
  - [`סיכום_איפיון_מלא_המערכת.md`](<source-materials/סיכום_איפיון_מלא_המערכת.md>) — המסמך המזוקק המלא, 33 ישויות.
  - [`עותק של מנהל_גיליונות1.xlsx`](<source-materials/עותק של מנהל_גיליונות1.xlsx>) — **מסמך האיפיון המקורי** (לא ייצוא נתונים). 20 גיליונות, ישות לכל גיליון, ברמת שדה: סוג שדה, חובה/רשות, ערכים אפשריים, הרשאות צפייה ועריכה לפי תפקיד, וטבלת תהליכים עם טריגרים. גיליון "לקוחות" לבדו מגדיר 82 שדות.
  - [`מסמך_מטרות_CRM_הנהלת_חשבונות_שכר_ומס.md`](<source-materials/מסמך_מטרות_CRM_הנהלת_חשבונות_שכר_ומס.md>) — מסמך מטרות-העל, בגרסת המרקדאון שהחליפה את ה-PDF. תוכנו מוטמע ב-`overview.md`; זהו העותק המקורי.
    **מעמד המסמך:** מקור סמכות ל**מה שהמשרד צריך להחזיק** — מלאי השדות, המשמעות העסקית, וכוונת ההרשאות. **אינו** מקור סמכות ל**מבנה**: הוא נכתב עבור אוריגמי, ולכן טיפוסי השדה, השדות המחושבים והצורה השטוחה שלו הם ארטיפקטים של אותה פלטפורמה — ר' `status.md`, סעיף "איך קוראים את גיליון האיפיון".
- `decisions/` — Architecture Decision Records:
  - [`0001-self-hosted-supabase-on-existing-vps.md`](decisions/0001-self-hosted-supabase-on-existing-vps.md) — למה self-hosted במקום Supabase Cloud.
  - [`0002-network-isolation-and-routing.md`](decisions/0002-network-isolation-and-routing.md) — למה תת-דומיין יחיד, איך Traefik מגיע ל-Supabase.
  - [`0003-vps-hardening-and-known-gap.md`](decisions/0003-vps-hardening-and-known-gap.md) — הקשחת SSH/fail2ban, ופער ידוע (פורט 3000).
  - [`0004-automated-backups-to-r2.md`](decisions/0004-automated-backups-to-r2.md) — גיבויים אוטומטיים ל-R2.
  - [`0005-rls-enabled-no-policies-yet.md`](decisions/0005-rls-enabled-no-policies-yet.md) — RLS מופעל מהרגע הראשון, בלי policies עדיין (חסום כברירת מחדל).
  - [`0006-lookup-table-vs-check-constraint.md`](decisions/0006-lookup-table-vs-check-constraint.md) — מתי רשימת ערכים היא טבלת lookup ומתי אילוץ CHECK.
  - [`0007-deletion-policy-restrict-and-soft-delete.md`](decisions/0007-deletion-policy-restrict-and-soft-delete.md) — restrict כברירת מחדל, ומחיקה רכה ללקוח.
  - [`0008-unified-channels-table.md`](decisions/0008-unified-channels-table.md) — איחוד שתי טבלאות הערוצים לאחת, ותיוג הערוץ הרשמי.
  - [`0009-definition-versus-runtime.md`](decisions/0009-definition-versus-runtime.md) — **הגדרה מול תפעול:** שלוש רמות (חוק / נוהל / עובדה), הפיצול בין צורת החוק לערכו, ומבחן "מה קורה כשזה משתנה". כולל את התקרה — איפה הגדרה הופכת לתכנות.
- `supabase-selfhost/scripts/` — כלי תחזוקה:
  - `verify.sh` — **בדיקת שלמות: ה-DB מול הריפו, וה-DB מול הכללים של עצמו.** שש עשרה בדיקות: RLS על כל טבלה, טריגר לכל `updated_at`, `security_invoker` לכל view, סנכרון `active_clients` מול `clients`, התאמת אובייקטים לקבצי entities, **ספירת הטבלאות המוצהרת ב-`entities/INDEX.md` מול ה-DB** (נוספה 14/09 אחרי שהכותרת התיישנה ל-37 בעוד ה-DB מחזיק 42), **מחריג מת** (דריסה שמורידה חובה שהשרשרת לא מייצרת — כשל), **לקוח פעיל בלי חובות כלל** (הערה ולא כשל: ייעוץ-בלבד זו תשובה לגיטימית, ורק אדם מבחין), **כיסוי האופק המוצהר** בלוח השנה ובחובות, **תזכורת לכל חובה פתוחה בתוך האופק**, **הסכמה על תדירות בין הוויו למחולל** (הבאג של 15/09 נבע בדיוק מאי-הסכמה, ואיש לא בדק), הופעת כל מיגרציה/seed/ADR באינדקס, וקישורים שבורים. מחזיר exit code — ניתן לחיבור ל-CI. **להריץ בסוף כל קבוצת ישויות ולפני כל קומיט שנוגע בסכימה:** `bash supabase-selfhost/scripts/verify.sh`
  - `test_deal_model_constraints.sql` — סוללת 17 בדיקות שליליות + 4 חיוביות למודל הקטלוג/עסקה, כל אחת ב-`SAVEPOINT` שמתגלגל לאחור. **לא נשאר ממנה כלום ב-DB**, בטוח להרצה חוזרת בכל עת: `cat supabase-selfhost/scripts/test_deal_model_constraints.sql | psql ...`
  - `test_obligation_template_constraints.sql` — 12 בדיקות שליליות + 3 חיוביות לתבניות החובה, באותו דפוס. הבדיקה החיובית המרכזית מריצה את **השרשרת המלאה** ומחזירה אילו חובות כל לקוח דוגמה באמת חייב. שתי בדיקות מאלצות את האילוץ הנדחה מוקדם ב-`SET CONSTRAINTS ... IMMEDIATE`, כי אחרת הוא נבדק רק ב-COMMIT.
  - `test_client_obligation_override_constraints.sql` — **11 שליליות + 8 חיוביות** לישות 11, באותו דפוס: כל בדיקה ב-`SAVEPOINT`, הכל בטרנזקציה שמתגלגלת לאחור, אפס דליפה (אומת בספירה לפני/אחרי). **הסוללה הזו תפסה באג אמיתי** — בדיקה N2 גילתה שהאילוץ המקורי החזיר `NULL` ולכן עבר.
- `supabase-selfhost/seeds/` — נתוני ייחוס ובדיקה, נפרד מהסכימה:
  - `0001_lookups.sql` — ערכי כל 11 טבלאות ה-lookup. **idempotent**, בטוח להרצה חוזרת. כל רשימה מסומנת במקורה: [מתועד] / [מקור] / [הצעה — לאישור יצחק].
  - `0002_periods.sql` — **שורות התקופה עצמן, 2024–2035** (228: 144 חד-חודשיות, 72 דו-חודשיות, 12 שנתיות). `periods` הייתה ריקה מיום יצירתה, כלומר שום דבר תקופתי לא היה ניתן לרישום. **idempotent** — האילוץ `unique (תדירות, מ, עד)` נושא את זה, ובטוח להרצה חוזרת גם אחרי הארכת הטווח. **חייב טרנזקציה אחת** (טבלת החודשים הזמנית היא `on commit drop`).
  - `0003_holidays_hebcal.sql` — **413 שורות, 2024–2030, מ-Hebcal** עם `i=on` (לוח ישראל — בלעדיו שבועות הוא יומיים). נמשך **פעם אחת ונקבע בקוד**, כי seed שתלוי ב-API חי אינו ניתן לשחזור. כל שורה נושאת `source='hebcal'` — **זה ההבדל מתקרות המס, שנשארות ריקות כי אין להן מקור כזה.** 63 ימים שאינם עבודה. **תיקון ידני יחיד:** יום העצמאות, שהוא שבתון בחוק ו-Hebcal מסמן `yomtov=false`. **idempotent** — הרצה חוזרת אימתה שהשורות הידניות לא נגעות.
  - `9001_scenario_smoke_test.sql` — חמישה תרחישי לקוח שמפעילים את הסכימה מקצה לקצה. **לא נתוני אמת ולא נזרע לצמיתות** — מיועד להרצה בטרנזקציה עם rollback.
  - `9002_cleanup_scenario_data.sql` — מוחק בדיוק את שורות התרחישים לפי מזהים מקובעים. **להריץ לפני טעינת לקוחות אמיתיים.** נבדק בהרצה עם rollback.
  - `9003_catalog_and_deal_scenarios.sql` — קטלוג דוגמה (6 שירותים, כל סוגי האספקה/תמחור), ומודל עסקה מלא על 5 מששת לקוחות התרחישים: חשבון פתוח, חשבון סגור עם לקוח שעזב, לקוח בלי חשבון בכלל, היסטוריית מחיר על אותה שורה, מדרגות עם היסטוריה, מכסה עם תעריף חריגה מוסכם, הנחה כללית+ספציפית, והטבה. **נתוני דוגמה, לא קטלוג אמיתי** — לנקות לפני פרודקשן כמו 9001.
  - `9004_obligation_template_scenarios.sql` — 4 רשויות ו-7 תבניות חובה שמכסות את **כל ארבעת יחסי הגומלין** עם הדוגמאות שהאיפיון נקב בהן (מקדמות = עצמאי, תשלום מע"מ = אחרי, ניכויים = מובלע, פנסיה = לפני-חוסם), הקישור שלהן לשירותים, וסינון סוג ישות על שתי תבניות המע"מ. **נתוני דוגמה** — התבניות האמיתיות ומה גורר מה הן הזנה של המשרד, לא מבנה.
  - `9005_obligation_override_scenarios.sql` — **שתי שורות דמו לשני הכיוונים שמעולם לא שרדו COMMIT:** מחריג (ברקוביץ נכסים בלי מקדמות — המקרה שהצדיק את הטבלה) וכולל (אבי מזרחי, דוח שנתי בטובה, לקוח שגוזר כלום ומקבל חובה אחת). דריסת התדירות כבר הייתה מהמיגרציה. **idempotent** — כל הוספה מוגנת ב-`NOT EXISTS`. נתוני דמו, לניקוי לפני פרודקשן.
- `supabase-selfhost/migrations/` — קוד ה-SQL בפועל, כולם הורצו בהצלחה על ה-DB החי:
  - `20260828000000_client_entity.sql` — 13 הטבלאות הראשונות.
  - `20260828010000_client_structured_address.sql` — פיצול כתובת לשדות מובנים.
  - `20260828020000_contacts_architecture.sql` — 7 טבלאות ארכיטקטורת אנשי קשר + עדכוני clients/office_employees/client_notes.
  - `20260828030000_client_contacts_history.sql` — שדות היסטוריה ל-`client_contacts`.
  - `20260828040000_office_employees_full.sql` — איפיון ראשוני ל-`office_employees` (פרטי קשר, ת.ז., תאריכי עבודה, סטטוס).
  - `20260828050000_office_employees_refine.sql` — תיקון: פרטים אישיים עברו ל-`contacts` (לא כפילות), נוספו תנאי העסקה אמיתיים, ונוספה `employee_absences` (היעדרויות זמניות, כיסוי בהחלטה אנושית).
  - `20260828060000_group_a_closeout.sql` — סגירת קבוצה א': `entity_types.turnover_threshold`, `tags.category`→`tag_categories` (lookup), שדות ביקורת ל-`client_tags`.
  - `20260830000000_periods_and_services_skeleton.sql` — התחלת קבוצה ב': `periods` (מלא), `services` (שלד מינימלי בלבד — יועמק בהמשך).
  - `20260831000000_audit_remediation.sql` — תיקון הביקורת הארכיטקטונית: RLS על 13 טבלאות שלא קיבלו אותו בקוד, איחוד ערוצים והזדהויות, יומן ביקורת גנרי, `restrict` + מחיקה רכה, המרות lookup, אילוצי תקינות. הורצה בטרנזקציה אחת אחרי dry-run.
  - `20260901000000_catalog_and_deal_model.sql` — העמקת קטלוג השירותים ומודל העסקה: `services` מלא, קטגוריות, תדירויות חיוב, מחירון ומדרגות, `deals`/`deal_services`/`deal_service_terms`, `discounts`, וה-View `deal_services_current`. מתקינה `btree_gist` לאילוצי EXCLUDE על טווחי תוקף. הורצה בטרנזקציה אחת אחרי dry-run, ואומתה ב-22 בדיקות תפקודיות.
  - `20260901010000_deal_terms_tiered_price_fix.sql` — תיקון שהתגלה תוך כדי בניית נתוני דוגמה: `deal_service_terms.agreed_price` היה `not null` בלי יוצא מן הכלל, מה שהפך שירות מדרגות לבלתי ניתן לייצוג (אין מחיר יחיד לשירות שמחירו נגזר מנפח). הורד ה-`NOT NULL`, נוסף טריגר שאוכף בכיוון ההפוך — ריק מחויב רק בשירות מדרגות, חובה בכל שירות אחר.
  - `20260908000000_entity_type_thresholds_by_year.sql` — תקרת המחזור עברה מעמודה בודדת ל-`entity_type_thresholds` **לפי שנה**. התגלה בדיוק לפני מילוי הערך: התקרה משתנה שנתית ו-`client_turnover_history` שומר לפי שנה, כך שערך בודד היה מחזיר תשובה שגויה לכל בדיקה היסטורית של חציית סף (מטרה 8). העמודה הישנה הוסרה — מעולם לא אוכלסה, בלי views תלויים.
  - `20260908010000_obligation_templates.sql` — **השכבה שסוגרת את השרשרת:** `authorities`, `obligation_templates` (מאחדת הגשה ותשלום, עם ארבעת יחסי הגומלין וכלל המועד), `service_obligation_templates` (מה שהופך את הקטלוג ממחירון למתכון), ו-`obligation_template_entity_types` (רשת הביטחון הרגולטורית). כולל טריגר מניעת קישור הדדי, ואילוץ נדחה דו-כיווני שאוכף שתבנית מוגבלת אכן מפרטת סוגי ישות — גם בהוספה וגם במחיקת השורה האחרונה.
  - `20260909000000_obligation_template_form_number.sql` — `form_number` על תבנית חובה (102, 126, 856). nullable, כי לחובת תשלום אין טופס. הצוות עובד לפי המספרים האלה וכל אינטגרציה עתידית תזדקק להם.
  - `20260910000000_threshold_kind.sql` — `entity_type_thresholds` קיבלה `threshold_kind`. נבנתה לתקרה אחת (עוסק פטור), ותשובת יצחק על תדירות מע"מ חשפה שנייה — סף הדיווח החודשי. זהות בצורה, שונות במשמעות, וכל אחת מפעילה התראה אחרת. `turnover_threshold` שונה ל-`amount`.
  - `20260914000000_turnover_and_thresholds_by_period.sql` — **`client_turnover_history` ו-`entity_type_thresholds` עברו מ-`year integer` ל-`period_id`.** ההכרעה התקבלה עקרונית ב-08/09 ובוצעה עכשיו, אחרי שנזרעו שורות התקופה. **תוספת מודעת מעבר למה שאושר:** טריגר `enforce_annual_period` שאוכף שהתקופה שנתית (`months_interval = 12`, לא התווית) — בלעדיו FK בודד היה **מחליש** את ה-`CHECK` שהוסר ומאפשר מחזור שנתי שמוצמד לינואר. המיגרציה נועלת את עצמה: שגיאה ונסיגה אם נותרה שורה שלא מופתה, במקום להשמיט שנה בשקט.
  - `20260914010000_client_obligation_overrides.sql` — **ישות 11.** הטבלה הדלילה שאומרת "אצל הלקוח הזה זה אחרת", בשלושה מקרים: מחריג · תדירות אחרת · כולל. שני `CHECK` ו**שלושה טריגרים** למה ש-`CHECK` לא יכול לבטא (תוקף הפוך, דריסה על תבנית תלויה, דריסות חופפות) — כולם נשענים על תאריכים שיושבים ב-`periods`. מורידה את שתי עמודות התדירות מ-`clients` (ישות 11 בצורה גרועה), מעבירה את השורה האחת שהייתה חריגה אמיתית, ובונה מחדש את `active_clients`.
  - `20260914020000_override_frequency_source_null_fix.sql` — **תיקון מלכודת `NULL` ב-`CHECK`.** האילוץ המקורי ל-`frequency_source` החזיר `NULL` כשהמקור היה ריק, ו-`CHECK` שמחזיר `NULL` **עובר** — כלומר ניתן היה להזין תדירות חורגת בלי מקור, השדה שקובע אם המערכת רשאית לשנות לבד. פוצל לשני אילוצים בטוחי-`NULL`. **נתפס בבדיקה N2.**
  - `20260915000000_client_obligations_current_view.sql` — **הוויו שסוגר את ישות 11.** עד אליו טבלת הדריסות הייתה טבלה שאף אחד לא קרא, והמשפט "הרשימה נגזרת ולא מאוחסנת" לא היה ממומש. מצליב שרשרת × דריסות, מיישם מחריג/כולל/תדירות, ו**מוריש קצב מעוגן לתבנית תלויה**. `security_invoker = true`, ורשימת העמודות מוקפאת — ר' `entities/client_obligations_current.md`.
  - `20260915010000_holidays.sql` — **טבלת החגים.** אינה אחת מ-33 הישויות; התגלתה בקריאת המקור לישות 30. **שתי שכבות:** `is_yomtov` גולמי מ-Hebcal (עובדה הלכתית) מול `is_non_working` שאדם מחזיק (החלטה מנהלית) — כי Hebcal יודע הלכה ולא מִנהל, ויום העצמאות הוא ההוכחה. `source` קובע מי הבעלים של השורה, **ואותה מכונה בדיוק כמו `frequency_source` בישות 11**. שבת אינה בטבלה — אריתמטיקה.
  - `20260915020000_regulatory_calendar.sql` — **ישות 30.** תאריך יעד לכל תבנית × תקופה, **בלי לקוח** — זה מה שמאפשר לדחייה אחת לעדכן 90 לקוחות. **שלוש שכבות נשמרות** (נוסחה / תיקון שבת-חג / דריסה) כי תאריך שאיש לא יכול להסביר הוא תאריך שאיש לא סומך עליו, ו-`effective_due_date` היא עמודה מחושבת שלעולם לא יוצאת מסנכרון. כולל `next_working_day()` ומחולל ש**מרענן את השכבות המחושבות ולא נוגע בדריסה** — **הפעם השלישית שהצורה הזו מופיעה** (אחרי `frequency_source` ו-`holidays.source`). טריגר מונע תבנית חודשית עם תקופה שנתית.
  - `20260915030000_obligations_at_date_function.sql` — הוויו `client_obligations_current` הפך ל-**פונקציה של תאריך**, `client_obligations_at(date)`, והוויו הוא עכשיו עטיפה דקה עליה ב-`current_date`. **לא קוסמטי:** מחולל שמייצר חצי שנה קדימה חייב לשאול מה הלקוח חייב **באותה תקופה** — לקוח ששירותו נגמר במרץ לא אמור לקבל חובות ביוני.
  - `20260915040000_obligations.sql` — **ישות 10.** (לקוח × שורת לוח שנה). המועד **מצטרף ולא מועתק** — הפוך מהמחיר, כי **מעתיקים כשהרשומה הונפקה לצד שלישי ומצטרפים כשהעובדה שייכת לצד שלישי**. הייחוד הוא **אינדקס חלקי** `where amends_id is null`, אחרת דוח מתקן חסום. "באיחור" **מחושב**. שלושה טריגרים: תאריך ביצוע מול סטטוס, תקינות תיקון, וחסימה לפי קודם. כולל `obligation_statuses` ואת הוויו `obligations_worklist`.
  - `20260915050000_calendar_covers_override_frequencies.sql` — **תיקון שני באגים שהתגלו רק כשייצרנו בפועל.** הלוח נוצר לפי תדירות התבנית בלבד, ולכן **דריסת התדירות של ישות 11 הייתה קוסמטית** — רוזנברג קיבל מע"מ דו-חודשי למרות דריסה חודשית. ובאג שני מסוכן יותר: **תשלום מע"מ שלו נעלם לגמרי**, כי הוא **יורש** תדירות מהעוגן ועל תבנית תלויה אסור לרשום דריסה. **חובה שנעלמת בשקט היא כשל מטרה 1.** אף אחד מהם לא נראה בוויו או בסוללות — כולן שואלות על תבנית בבידוד.
  - `20260915060000_office_settings.sql` — **ישות 33.** שורה אחת, נאכפת ב-`unique check`. **עמודות מוטיפסות ולא מפתח-ערך**, וההכרעה נגזרת מ-`ADR 0009` עצמו: **ערך של הגדרה הוא נתון** (המשרד משנה, `UPDATE`), **קיום של הגדרה הוא מבנה** (קוד חייב לקרוא אותה) — ולכן הגדרה חדשה דורשת קוד בכל מקרה, ומפתח-ערך רק היה מסתיר את זה. ובונוס: אילוצים אמיתיים, כך ש"ימי תזכורת = 3-" נחסם.
  - `20260915070000_reminders.sql` — **ישות 34, וסגירת המסלול של מטרה 1.** שורה לכל חובה, ו**האיחוד להודעה אחת ללקוח ליום הוא הוויו `reminders_due`** — 77 תזכורות → 38 הודעות. נבדק שהאיחוד נחוץ כבר בתוך החובות עצמן: לרוזנברג ארבעה מועדים בפברואר שנופלים על אותו יום התראה. `obligation_id` הוא `not null` **במכוון** — צומת עם צד אחד קיים הוא הכלל שנשבר שלוש פעמים, וכשחיובים יתקיימו העמודה תהפוך nullable באותה מיגרציה. חובה שהושלמה נושרת מהרשימה **בלי טריגר**, כי הוויו מסנן על הסטטוס.
  - `20260915080000_audit_the_money_tables.sql` — **יומן הביקורת מגיע לכסף.** זוהה בבחינה של 14/09 כ"תיקון זול" ולא בוצע: הביקורת ישבה על שתי טבלאות בלבד, **לא על `deals`, `deal_services`, `deal_service_terms` ו-`discounts`** — בעוד מטרה 9 היא "אפס מחלוקות", **והמחלוקת הסבירה עם לקוח היא על מחיר**. ⚠️ **ופער שני שחמור כאן במיוחד:** הטריגר ירה על `UPDATE` בלבד, ו**שינוי מחיר מתבטא כשורה חדשה ולא כעדכון** — כלומר ביקורת על טבלאות הכסף הייתה רושמת **כלום** בדיוק על האירוע שבשבילו היא קיימת. הפונקציה מטפלת עכשיו בשלוש הפעולות, ושני הטריגרים הקיימים נבנו מחדש מעליהן — **מה שסוגר את הפער "יצירה ומחיקה אינן מתועדות"**. קריאה עדיין לא נתפסת: טריגר לא רואה `SELECT`.
  - `20260915090000_generation_horizon.sql` — **`generation_horizon_months` ב-`office_settings`.** שלושה מחוללים קיימים, וכמה קדימה כל אחד צריך להגיע היה **מספר שהקלדתי בקריאה**. **התזמון הוא אוטומציה** (הערת יצחק, מקובלת), אבל **היעד חייב להיות מוצהר** כדי שהאוטומציה לא תמציא אותו — ו**אוטומציה שנשברה בשקט נראית בדיוק כמו אוטומציה שאין לה מה לעשות**, ולכן `verify.sh` צועק. כל השכבות הועלו לאופק באותה מיגרציה.
