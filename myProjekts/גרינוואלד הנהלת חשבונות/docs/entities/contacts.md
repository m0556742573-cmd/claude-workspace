# טבלה: `contacts` (אנשי קשר — ישות 2 במודל המקורי)

## מה זה ולמה זה משרת

מייצגת בני אדם (ולעיתים מוסדות) — **חוצת-לקוחות במפורש**: אותו אדם (למשל רואה חשבון שמטפל בכמה לקוחות שלך) הוא שורה **אחת** כאן, לא מועתק לכל לקוח. יכול להיות איש קשר של הלקוח (בעלים, מזכירה) או גורם חיצוני (רו"ח מבקר, פקיד שומה, נציג בנק). מחזיקה רק פרטים **כלליים** שרלוונטיים לכל אדם — פרטים ספציפיים לתפקיד/לקוח מסוים יושבים על `client_contacts`, לא כאן (ר' שם).

## שדות

| עמודה (אנגלית) | תרגום השם | טיפוס | אילוצים | מה זה משרת |
|---|---|---|---|---|
| `id` | מזהה | uuid | PK | מזהה ייחודי |
| `first_name` | שם פרטי | text | NOT NULL | |
| `last_name` | שם משפחה | text | nullable | ריק עבור מוסדות/גורמים לא-אישיים |
| `id_number` | תעודת זהות | text | nullable, **text לא integer** (אפסים מובילים) | PII, לא תמיד רלוונטי (למשל למוסד) |
| `street_address` | רחוב ומספר | text | nullable | כתובת פרטית של האדם — לא כתובת העסק (זו על `clients`) |
| `city` | עיר | text | nullable | |
| `postal_code` | מיקוד | text | nullable, **text לא integer** | |
| `created_at` | תאריך יצירה | timestamptz | NOT NULL, DEFAULT now() | ביקורת |
| `updated_at` | תאריך עדכון אחרון | timestamptz | NOT NULL, DEFAULT now(), טריגר | ביקורת |

## ר' גם

[`channels.md`](channels.md) — אמצעי הקשר, בטבלה מאוחדת: ערוץ של איש קשר מסומן ב-`contact_id` (ולא ב-`client_id`). החליף את `contact_channels` הנפרדת — ר' `decisions/0008`. [`client_contacts.md`](client_contacts.md) — הקשר בין איש קשר ללקוח ספציפי (תפקיד, שדות מיוחדים).
