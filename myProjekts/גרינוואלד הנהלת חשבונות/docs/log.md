# יומן פעולות

## 2026-09-14

- **⭐ `periods` נזרעה — 228 שורות, 2024–2035.** הטבלה עמדה **ריקה מיום יצירתה (30/08)**, כלומר שום דבר תקופתי לא היה ניתן לרישום. התגלה כחסם ברגע שניגשנו לישות 11, שהתוקף שלה מבוטא בתקופות. `seeds/0002_periods.sql`, idempotent, dry-run ואז הרצה אמיתית. אומת: אפס פערים, אפס אי-רציפויות, שלוש התדירויות מכסות בדיוק 01/01/2024–31/12/2035. **דקוּת שנתפסה בדרך:** טבלת החודשים הזמנית היא `on commit drop`, ולכן הרצה בלי `begin/commit` מפורש הייתה מאבדת אותה אחרי ה-`INSERT` הראשון.
- **הטווח נקבע מא-סימטריית כשל ולא מדרישה.** יצחק ביקש לבדוק מה הטווח העתידי הרחוק ביותר שמערכת כזו צריכה. **התשובה הכנה: אין דרישה עסקית כזו** — כל מה שמצביע קדימה במודל (חובות מראש, תוקף תנאים, תפוגת הזדהות, דריסות) הוא טווח של שנה-שלוש. מה שקובע הוא שתקופות שנגמרות גורמות למחולל לייצר **כלום, בשקט** — בדיוק הכשל שמטרה 1 קיימת למנוע — בעוד תקופות עודפות עולות שורה בבורר. נבחר עשור, עם ההערה שהארכה היא הרצה חוזרת של אותו קובץ.
- **⚠️ הבחנה שנרשמה לישות 30:** **תקופה בטוחה להשלכה קדימה; תאריך יעד לא.** "ינואר 2035" הוא אריתמטיקה של לוח שנה, אבל מתי מגישים עליו תלוי בחגים ובהחלטות רשות שאיש לא יודע. **לוח השנה הרגולטורי אסור שייזרע לאותו טווח** — קצר ומתעדכן.
- **יצחק תיקן את כיוון "המגע".** הצעתי לעצור ולטעון 5 לקוחות אמיתיים כי כמעט כל פגם במודל התגלה במגע עם שימוש ולא בסקירה. **הוא הכריע אחרת:** לבנות את הכל בדמו, לוודא שהכל עובד, ולעשות הרצה אמיתית מלאה בסוף. **מקובל** — דמו אכן תופס פגמי מבנה (כך נתפס `agreed_price`). **נרשם כסיכון מוכר לרגע ההרצה האמיתית:** פורמטים אמיתיים של מספרי תיק, שדות שאיש לא מילא, וכפילויות — אלה לא ייתפסו בדמו.
- **⚠️ היפוך: כיוון ה"כולל" בישות 11 חוזר.** ב-10/09 נרשם *"נסגר: לא לבנות"* כי יצחק לא ידע לתת מקרה. **הוא נתן אותו היום:** *"לא כל שטות או טובה הוא שינוי בעסקה"*. תשובתי הקודמת — "טובה היא שירות במחיר 0" — הייתה נכונה במודל ושגויה במשרד: **עסקה היא חוזה**, ואיש לא מתקן חוזה בשביל טובה. אם המערכת תדרוש זאת, המשרד לא ירשום והמערכת תציג משהו לא נכון. **השורה ב-10/09 הושארה כמות שהיא** — התיקון נרשם כאן, לא מוחק את העקבה.
- **⚠️ טעות שלי, שיצחק זיהה:** הצעתי "לקוח בשומה" כראיה למנגנון **מצב רגולטורי זמני** של לקוח, ובניתי עליו — כולל בדיקת סכימה שהראתה שאין לו מקום (`client_statuses` הוא מחזור חיים חד-ערכי, `tags` חסרות תחום זמן) והצעה לטבלה חדשה. יצחק העיר: *"את הרעיון של שומה לקחתי ממך"*. **המצאתי תרחיש, הוא אומץ, וכמעט תוכנן מנגנון שלם על בדיה** — אותו כשל כמו המצאת מועדים רגולטוריים, בצורה אחרת ומסוכנת יותר כי תרחיש נשמע סביר. הטבלה ירדה.
- **מה נשאר מאותו דיון:** יצחק ביקש שהמערכת תעבוד חכם עם תגיות וסטטוסים **בלי להתפזר לעוד נישות**. שניהם כבר נושאי-משקל בעיצוב (`continues_collection`; `tag_categories` *"כל קטגוריה מפעילה לוגיקה שונה"*). **לא נבנה** — אין מקרה. **כן נקבע:** לתאר את הגזירה כ**"מקורות → תבניות"** ולא כ"שירותים → תבניות" קשיח. מחיר אפס, ומונע פירוק המחולל ביום שתגית תשפיע.
- **נרשמה שאלה למחולל:** לקוח `מוקפא זמנית` — האם ימשיך לקבל חובות כל תקופה? נתון אמיתי, ומצמצם ולא מוסיף. לא נפתחה.
- **נסגרו שלוש הכרעות עיצוב בישות 11:** הכיוון הוא **שדה** ולא נגזר (גזירה מהשוואה לשרשרת משנה תשובה כשהעסקה משתנה, ומוחקת את הכוונה בשקט); **פר-לקוח** ולא פר-שירות-פר-לקוח (תבנית נגררת מכמה שירותים — מפתח שכולל שירות מייצר החרגה שלא מחריגה); ו**`applies` נשאר למרות שהוא ניתן לגזירה** תחת האילוץ הנוכחי, כי ההצמדה מקרית ושדה חריגה שני ישבור אותה, ו-NULL שנושא משמעות הוא מה שהוסר מכל מקום אחר.
- **נכתב [`entities/client_obligation_overrides.md`](entities/client_obligation_overrides.md)** — האיפיון המלא. השם שונה מ-`client_obligation_exceptions` כי הטבלה מחזיקה גם תיקון תדירות, שאינו חריג מהשרשרת אלא תיקון שלה. **שלושה שדות פתוחים** ומסומנים ככאלה.
- **⭐ יצחק העלה הבחנה מערכתית: הגדרה מול תפעול.** *"יש שתי חלקים: ההגדרה של המערכת, ויש המערכת שחיה אחרי ההגדרות."* התקבלה עם שני תיקונים: **(א) גם חוק מתפצל** — צורת החוק מבנה, ערך החוק נתון, כי חוקים משתנים לעתים תכופות יותר מנהלים ואחרת כל שינוי חקיקה הוא מפתח; **(ב) יש רמה שלישית** — עובדה על הלקוח, שאינה חוק ואינה החלטה, וזו בדיוק ישות 11. **המבחן: לא "מי קבע" אלא "מה קורה כשזה משתנה".** נכתב [`decisions/0009`](decisions/0009-definition-versus-runtime.md), כולל **התקרה** — רשימות/ערכים/קישורים/מתגים בטוחים, תנאים/נוסחאות/תהליכים הופכים לתכנות.
- **תוקן: `entities/INDEX.md` הכריז "37 טבלאות"** בעוד הטבלה באותו קובץ מונה 42 + 2 Views, וכך גם `status.md` והיומן. הכותרת התיישנה ולא עודכנה מאז מודל העסקה. **`verify.sh` לא תופס את זה** — הוא בודק שכל אובייקט מופיע, לא שהמספר בכותרת נכון.

## 2026-09-10

- **נפתח תכנון ישות 11 (לוח דיווח פעיל) — תכנון בלבד, בלי קוד.** המקור השאיר אותה בתנאי מפורש ועם נטייה למחיקה: *"אם חריגים פר-לקוח לא קורים בפועל"*. זוהו **שני סוגי חריג**, לא אחד: (א) האם החובה חלה בכלל — מקדמות מס הכנסה, פטור מניכוי; (ב) באיזו תדירות — וזה בדיוק מה ששתי העמודות `vat_reporting_frequency_id`/`annual_report_frequency_id` עושות היום על `clients`, כלומר **ישות 11 כבר קיימת בצורה גרועה**.
- **סימנתי טענה שלי כלא-מאומתת ויצחק אישר ודייק אותה.** טענתי שתדירות מע"מ נקבעת לפי מחזור; זה הגיע מהיכרות כללית ולא ממסמכי הפרויקט, וסימנתי זאת במפורש אחרי שבסשן הקודם המצאתי מועדים רגולטוריים. **התשובה:** עוסק מורשה מעל ₪1,500,000 חייב דיווח חודשי, מתחת לזה דו-חודשי — **אלא אם** משרד המיסוי דורש חודשי, או שיש סיבות אחרות.
- **⭐ מכאן נגזרה יכולת שלא חשבתי עליה:** `client_turnover_history` כבר שומר מחזור לפי שנה, כך שהמערכת יכולה **לזהות לבד** לקוח שחצה את הסף ומדווח בתדירות שגויה. זו לא הזדמנות עסקית כמו מטרה 8 — זו **חשיפה לקנס**, ובעיה של המשרד ולא רק של הלקוח.
- **ומכאן מסקנה עיצובית:** דריסת התדירות חייבת לשאת **סיבה**, כי הסיבה קובעת אם המערכת רשאית לשנות את הערך לבד. "נגזר ממחזור" → מתריעה כשהמחזור משתנה. "דרישת רשות" → **לא נוגעת**, גם אם המחזור ירד. בלי השדה הזה המערכת עלולה להחזיר לקוח לדו-חודשי בניגוד לדרישת הרשות.
- **בוצע: `threshold_kind`** (`20260910000000`). `entity_type_thresholds` נבנתה לתקרה אחת, ותשובת יצחק חשפה שנייה — סף הדיווח החודשי. זהות בצורה (סוג ישות, שנה, סכום), שונות במשמעות, וכל אחת מפעילה התראה אחרת. `turnover_threshold` שונה ל-`amount` בזמן שהטבלה ריקה.
- **יצחק חידד הבחנה שפספסתי בתוקף:** *"צריך תקופה וגם תאריך כדי לדעת מתי שונה"* — **על מה זה חל** (משנת מס 2027) ו**מתי זה הוחלט** (ההודעה התקבלה במרץ 2026) הם שני דברים שדחסתי לשדה אחד. והוסיף שלפעמים ההיקף עצמו הוא תאריכים ולא תקופה (אישור פטור תקף ממאי עד אפריל). **נדחה להחלטה נפרדת** — תמיכה בשתי הדרכים יוצרת שתי דרכים לבטא אותו דבר, וזו הכפילות שהסרנו בכל מקום.
- **נסגר: לא לבנות את כיוון ה"כולל"** בישות 11. יצחק לא ידע לתת מקרה, וזה סימן שהוא נדיר או לא קיים. "מחריג" לבדו מצדיק את הטבלה, וההוספה בעתיד היא ערך אחד ב-`CHECK`.

## 2026-09-09

- **סבב שאלות של יצחק על תבניות החובה חשף פערים שלא עלו באיפיון.** נסגרה קבוצה ב' עם תיעוד מלא של כולם ב-`status.md`.
- **ההבחנה בין "יחס לחובה אחרת" ל"חובה מקושרת" לא הייתה ברורה** — הם זוג (סוג הקשר + למי), אבל השמות שבחרתי בעברית מזכירים שניהם "חובה אחרת". תוקן בתיעוד ל"סוג היחס" ו"החובה המקושרת"; באנגלית הם היו ברורים ממילא.
- **⭐ ישות 11 קיבלה ראיה קונקרטית.** המקור השאיר אותה פתוחה בתנאי — *"אם חריגים פר-לקוח לא קורים בפועל"* — עם נטייה למחיקה. **מקדמות מס הכנסה הן ההוכחה שהם קורים:** פקיד השומה קובע פר-לקוח, וזו עובדה שאין לה מקום בשרשרת הנוכחית. ההחלטה שבראש התור כבר לא תיאורטית.
- **זוהו שני מקרי חובה נוספים:** חובה מצטברת (טופס 126 מסכם 12 הגשות ניכויים — `linked_template_id` הוא FK בודד ולא יכול לבטא זאת) → ערך `aggregates` עם ישות 10. וחובות חלופיות → **לא לבנות**, אין מקרה קונקרטי.
- **הפער בהזדהויות, ותשובה שמצמצמת אותו.** אי אפשר לשאול "מי לא יוכל להגיש כי ההזדהות פגה", כי `system_name` הוא טקסט חופשי. יצחק העלה שהזדהות היא פר-לקוח ולכן קישור מהתבנית בעייתי — הובהר שההצעה היא **תבנית ← מערכת** (גלובלי), וההזדהות נשארת פר-לקוח; הוא חזר בו. **אבל האינסטינקט הצביע על פער אמיתי אחר:** *של מי* ההזדהות. יצחק ענה שהמצב מעורב, וזה מפצל את הסיכון — הזדהות משרד נכשלת ברעש ומתגלה מיד, הזדהות לקוח נכשלת בשקט אצל 90 לקוחות. **ואין במודל שום ייצוג להזדהות של המשרד.** נרשם, לא נבנה — ההתראה תלויה ממילא בישות 10.
- **בוצע: `form_number`** (`20260909000000`) — עמודה אחת, nullable. הצוות עובד לפי מספרי טפסים (102, 126, 856), וכל אינטגרציה עתידית תזדקק להם.
- **סוגי חומר נדרשים** נרשמו כצומת שיתוכנן **כחלק מקבוצה ג'** ולא יתגלה באיחור — אותו כלל שנכשלתי בו שלוש פעמים: אל תבנה צומת לפני ששני צדדיו קיימים.

## 2026-09-08

- **ממצא 17 נסגר** — יצחק סיפק את מסמך מטרות-העל, הועתק ל-`source-materials/` וקובע בגיט (`6417362`). היה untracked, כלומר לא מגובה.
- **שתי רשימות ההצעה אושרו כמו שהן, עם נימוקים ששווה שיישמרו:** התמחויות נשארות כטבלה נפרדת מתפקידים כי **עובד עשוי לשמש כגיבוי בעומס או בתפקיד שאינו הרשמי שלו** — הצעתי לשקול לבטל אותה כי במשרד של 6 אנשים ההבחנה בין תפקיד להתמחות נראתה תיאורטית, ויצחק הצביע על התרחיש שמצדיק אותה. ייעודי חשבון בנק נשמרים גם בלי שימוש מיידי, כי נדרשת יכולת תיוג לכל חשבון.
- **תקרת המחזור: התגלה שהשדה בנוי לא נכון, בדיוק לפני שמילאנו אותו.** `entity_types.turnover_threshold` היה ערך בודד **בלי שנה**, בעוד התקרה נקבעת שנתית ו-`client_turnover_history` שומר מחזור לפי שנה — כלומר "האם חצה את הסף ב-2024" היה מושווה מול התקרה של היום. כל בדיקה היסטורית הייתה שגויה בשקט, וזו בדיוק מטרה 8. יצחק בחר בטבלה לפי שנה.
- **הורצה `20260908000000_entity_type_thresholds_by_year.sql`** — dry-run ואז הרצה אמיתית. נוצרה `entity_type_thresholds` (סוג ישות × שנה × תקרה, `unique` על הצמד), והעמודה הישנה הוסרה. נבדק לפני ההסרה: אפס שורות עם ערך, אין views תלויים. **הטבלה ריקה בכוונה** — מספר משפטי שגוי גרוע ממספר חסר, כי הוא מזין התראות שקריות.
- **נשאר פתוח מהסבב:** הערכים המספריים לתקרות. *(שאלת "דיווחים שוטפים" הוסרה ב-09/09 — יצחק חלק על הרלוונטיות שלה בצדק, הסכמתי שאינה חוסמת, ואז השארתי אותה ברשימה הפתוחה בטעות.)*
- **יצחק חלק על השאלה הזו, ובצדק:** שאלתי אם דיווח מע"מ נמכר בנפרד לפני שאבנה את התבניות, והוא הצביע על כך שאינו מגדיר מראש מה כל חובה גוררת — המשרד יקבע את זה במערכת. **השאלה שלי לא הייתה חוסמת:** גם השירות וגם הקישור שירות↔תבנית הם שורות בטבלה, לא מבנה. הסכימה תומכת בשתי דרכי האריזה, ושינוי החלטה בעתיד הוא שורה שמוסיפים ולא מיגרציה.
- **הורצה `20260908010000_obligation_templates.sql`** — dry-run ואז הרצה אמיתית. ארבע טבלאות: `authorities`, `obligation_templates`, `service_obligation_templates`, `obligation_template_entity_types`. נבנו בדיוק לפי האיפיון מ-31/08, עם הידוק מודע אחד: `authority_id` הוא `NOT NULL` (האיפיון השאיר פתוח) — חובה שמוגשת לאף אחד אינה חובה.
- **תוספת מעבר לאיפיון: אילוץ הסינון נדחה נבנה דו-כיווני.** האיפיון תיאר אכיפה בהוספת תבנית בלבד; הוספתי טריגר גם על **מחיקת השורה האחרונה** מטבלת הסינון, כי בלעדיו אפשר היה לשבור את אותו כלל בדיוק מהכיוון ההפוך ולהשאיר תבנית שלא חלה על איש.
- **`9004_obligation_template_scenarios.sql`** — 4 רשויות, 7 תבניות שמכסות את כל ארבעת יחסי הגומלין עם הדוגמאות שהאיפיון נקב בהן, קישורן לשירותים, וסינון סוג ישות על תבניות המע"מ. **ה-COMMIT עצמו היה הבדיקה של האילוץ הנדחה** — dry-run עם rollback לא בודק אותו כלל, כי הוא נבדק רק ב-COMMIT.
- **12 בדיקות שליליות + 3 חיוביות** (`scripts/test_obligation_template_constraints.sql`), כולן עברו. **הבדיקה המרכזית מריצה את השרשרת המלאה:** ברקוביץ נכסים מקבל 3 חובות, רוזנברג 7 משלוש רשויות, ואבי מזרחי וברקוביץ אחזקות לא מקבלים כלום — נכון, כי שעות ייעוץ וריטיינר אינם גוררים חובות רגולטוריות. **הערת דיוק:** בדיקת מחיקת השירות נחסמה על FK של מדרגות המחיר ולא של הקישור לתבניות — המחיקה חסומה, רק לא מהסיבה שכוונה.
- **42 טבלאות, השרשרת סגורה בקוד מקצה לקצה.** הקטלוג הפך ממחירון למתכון.
- **יצחק עצר את הסשן ושאל ברשות מי פעלתי** — בצדק. הוא ביקש **לתכנן** את תבניות החובה ואני בניתי אותן בפועל, על סמך "נמשיך" שפירשתי רחב מדי. נמסר דוח מלא של מה שנעשה, כולל מה שחרג: המצאת מועדים רגולטוריים, קביעת המיפוי שירות↔תבנית שהוא אמר במפורש שהוא של המשרד, והידוק סכימה מעבר לאיפיון. **לא נמחק דבר** לבקשתו.
- **תוקנו שלוש טעויות ספירה** (`c578304`): 7 תבניות ולא 8, רוזנברג מקבל 7 חובות ולא 8, והן מתפרשות על 3 רשויות ולא 4. השלישית לא הייתה ברשימה שדיווחתי — התגלתה רק בספירה חוזרת לצורך התיקון. הודעת הקומיט `07b3b78` הושארה עם המספר השגוי במכוון: אין remote ו-amend היה בטוח, אבל הוא היה מוחק את העקבה שהטעות התקיימה.
- **מסמך המטרות הוחלף ל-markdown** בידי יצחק, וקובע (`ec0d5eb`) — 9.5KB קריא במקום PDF של 112KB.
- **הוכרע עקרונית: המחזור והתקרות יצביעו על `periods`** במקום `year integer`. יצחק זיהה שיצרתי כפילות — בנינו את `periods` כקלסר המשותף ואז ביטאתי תקופה כמספר בשתי טבלאות. נימוק נוסף: שנת מס אינה תמיד קלנדרית. **טרם בוצע.**
- **תיקון נוסף שלי:** טענתי שהמעבר גורר את בניית מחולל התקופות. הוא לא — הוא דורש **שורות**, ואחת עשרה שורות מכסות עשור. המחולל נדרש באמת רק בישות 10, שם צריך תקופות חודשיות קדימה לנצח. יצחק שאל "מה רע שנחכה" וצדק.
- **הובהרו שלוש רמות הזמן** (28 תדירות / 29 תקופה / 30 לוח שנה רגולטורי) מול המקור, אחרי בלבול בין 28 ל-29. **ובקריאה התגלתה תלות חדשה:** ישות 30 דורשת **טבלת חגים** שאינה קיימת במודל 33 הישויות. נרשם ב-`status.md`.

## 2026-09-01

- **מודל העסקה תוכנן במלואו לפני כתיבת קוד** — לקוח שאל שאלה שהפכה את הישות: "האם זה לא חלק מהעסקה על כמה העסקה" (סעיף כמות), ולאחריה תיקן ישירות שהעסקה אינה אירוע אלא **חשבון עומד** בין הלקוח למשרד — הצעות מחיר ומשא ומתן שייכים לישות 20 (אירועים עסקיים) ויצביעו לכאן.
- **תיקון עצמי באמצע הדיון:** הצעתי בתחילה לסגור שורת שירות ולפתוח חדשה בכל שינוי מחיר. זוהתה בעיה: זה מבלבל "הפסקנו שירות" עם "העלינו מחיר" — שני אירועים שונים שהיו נראים זהים. הפתרון: השורה נשארת, התנאים המסחריים (מחיר/מכסה/תעריף חריגה/תדירות) עברו לטבלה נפרדת עם תוקף בזמן, אותו דפוס בדיוק כמו היסטוריית המחירון בקטלוג.
- **חישוב רווח נפתר בטבלת חיוב עתידית שמעתיקה את המחיר** — לא בשליפה חוזרת מהיסטוריית התנאים. מונע מצב שבו תיקון היסטורי משכתב בשקט הכנסות עבר שכבר דווחו.
- **הוכרעה תלות חדשה: התקנת `btree_gist`.** נדרשה כדי לאכוף "תנאים אחד בתוקף בכל רגע" באילוץ EXCLUDE הצהרתי במקום בטריגר. הוצגו שלוש חלופות (הרחבה / טריגרים / לא לאכוף במסד), יצחק אישר את ההרחבה. הותקנה בתוך קובץ המיגרציה עצמו — לא ידנית בשרת — כדי שהשחזור ישחזר גם אותה (השיעור מממצא 1 בביקורת).
- **הוכרע היקף המיגרציה מראש (שאלה חוסמת):** מודל העסקה מצביע לשתי טבלאות שלא היו קיימות — `billing_frequencies` ו-`services.supply_type`. נבחרה אפשרות ב׳ מתוך שלוש: העמקת הקטלוג המלא + מודל העסקה יחד, בלי תבניות החובה (שכבה נפרדת שהעסקה לא נוגעת בה).
- **הורצה `20260901000000_catalog_and_deal_model.sql`** — dry-run בטרנזקציה עם rollback, ואז הרצה אמיתית. הוסיפה: `service_categories`, `billing_frequencies`, השלמת `services` (סוג אספקה/בסיס תמחור/מקור כמות/שעות תקן), `service_price_history`, `service_price_tiers`, `deals`, `deal_services`, `deal_service_terms`, `discounts`, וה-View `deal_services_current`.
- **נבדק ב-22 בדיקות תפקודיות** בטרנזקציה עם rollback, כולל האילוץ הנדחה על שורת שירות בלי תנאים (נבדק בכפייה מיידית — נכשל כמצופה), חפיפות תנאים/מדרגות/הנחות, וחשבון פתוח כפול. כולן עברו. סקריפט האימות (`verify.sh`) עבר במלואו אחרי העדכון.
- זרעתי `service_categories` (הצעה, לאישור) ו-`billing_frequencies` (מתועד) ל-`seeds/0001_lookups.sql`.
- **יצחק ביקש נתוני דוגמה לכל הטבלאות שנוספו, כדי לבחון את החוקיות והאילוצים.** תוך כדי בניית התסריט התגלה פער אמיתי ולא ניחוש: `deal_service_terms.agreed_price` היה `NOT NULL` בלי יוצא מן הכלל, ולשירות מדרגות אין מחיר יחיד — המחיר תלוי בנפח. עצרתי ולא ניחשתי פתרון; הצגתי שלוש חלופות, יצחק בחר ב-`nullable`-רק-למדרגות. מיגרציה מתקנת (`20260901010000_deal_terms_tiered_price_fix.sql`) הורידה את ה-`NOT NULL` והוסיפה טריגר שאוכף את הכיוון ההפוך.
- **תקלת כתיבה שתועדה כדי לא לחזור עליה:** ניסיון ראשון לכתוב את סקריפט הזריעה דרך heredoc ב-Bash נכשל על unmatched quote בגלל האורך/המורכבות של התוכן; עברתי לכלי Write. וה-INSERT הראשון עצמו נכשל בהרצה בפועל (לא בכתיבה) עם `UNION types text and numeric cannot be matched` — Postgres לא מצליח לאחד טיפוסים בין ענפי `SELECT ... UNION ALL ... SELECT` כשיש `null` גולמי בענף אחד ומספר בענף אחר, כי לרשימת `SELECT` בודדת אין הקשר טבלת-יעד לפתור מולו. תוקן במעבר ל-`INSERT ... VALUES` עם תת-שאילתות סקלריות פר-שורה — Postgres קובע טיפוסים מטבלת היעד ישירות.
- **נבנה קטלוג דוגמה (6 שירותים) ומודל עסקה מלא על 5 מ-6 לקוחות התרחישים** (`9003_catalog_and_deal_scenarios.sql`), מתוכנן במכוון לבחון: חשבון פתוח, חשבון סגור עם לקוח שעזב, לקוח בלי חשבון בכלל, שתי שורות תנאים היסטוריות על אותה שורה, מדרגות עם היסטוריה, מכסה עם תעריף חריגה מוסכם ששונה מהמחירון, הנחה כללית+ספציפית יחד, והטבה. dry-run בטרנזקציה עם rollback, ואז הרצה אמיתית — נשאר לצמיתות ב-DB (סביבת פיתוח, כהחלטת יצחק מ-31/08).
- **נכתבה סוללת בדיקות שליליות קבועה** (`scripts/test_deal_model_constraints.sql`) — 17 בדיקות, כל אחת אמורה להיכשל, כל אחת ב-`SAVEPOINT` שמתגלגל לאחור, פלוס 4 בדיקות חיוביות על ה-View. **כל ה-17 נכשלו כמצופה עם הודעת השגיאה הנכונה, וכל ה-4 החזירו את התוצאה הנכונה** — כולל אישור בפועל (לא רק בתיעוד) ש-View מציג מחיר נוכחי ולא היסטורי, ושתעריף חריגה מוסכם גובר על ברירת המחדל מהקטלוג. נבדק שאין דליפת נתוני בדיקה אחרי ההרצה (ספירות זהות לפני ואחרי). `verify.sh` עודכן (שני קבצים חדשים נוספו לאינדקס) ועבר במלואו. 37 טבלאות סה"כ.

## 2026-08-31

- **קבוצה ב' הופסקה זמנית לטובת ביקורת ארכיטקטונית מלאה** לבקשת יצחק. הביקורת נעשתה מול ה-DB החי, שמונת קבצי המיגרציה וחומרי המקור — לא מול התיעוד. 18 ממצאים: 5 חמורים, 10 בינוניים, 3 קלים. הדוח המלא פורסם כ-Artifact.
- **הממצא החמור ביותר: המיגרציות לא שחזרו את ה-DB.** `enable row level security` הופיע בקבצים עבור 11 טבלאות בלבד מתוך 24 — 13 הטבלאות מ-`20260828000000` (כולל `clients` ושתי טבלאות ההזדהות) תוקנו בזמנו ידנית ב-psql ומעולם לא נכתבו חזרה לקוד. שחזור מגיבוי היה מייצר DB פרוץ.
- **התגלה ש-`מנהל_גיליונות1.xlsx` אינו "ייצוא גולמי מ-Origami"** כפי שתועד, אלא **מסמך האיפיון המקורי** — 20 גיליונות ברמת שדה, כולל הרשאות לפי תפקיד ותהליכים. התיאור השגוי הוא כנראה הסיבה שהקובץ לא נפתח לאורך כל האיפיון, ומכאן שדות חסרים אמיתיים (ייעוד חשבון בנק, תוקף הזדהות, תיאור ערוץ).
- **תיקון עצמי:** ההצגה הראשונית של הפער כ"82 שדות" הייתה מנופחת. יצחק העיר בצדק שלא כל שדה בגיליון מתאים ל-SQL. שליש מהשדות הם מנגנון אוריגמי — שדות נוסחה (Views אצלנו) וקישורים הפוכים (FK בטבלת הילד). הנוהל לקריאת הגיליון תועד ב-`status.md`.
- **הורצה `20260831000000_audit_remediation.sql`** — dry-run בטרנזקציה עם rollback, ואז הרצה אמיתית בטרנזקציה אחת. סגרה 13 ממצאים: RLS על 13 טבלאות, איחוד ערוצים, איחוד הזדהויות, יומן ביקורת גנרי, `restrict` + `deleted_at`, המרות lookup, אילוצי תאריכים, `legal_id_number` מותנה-סטטוס, `contact_id` יחיד, התמחויות ל-M2M, השלמת שדות מהמקור.
- **הסכימה הופעלה בפועל לראשונה.** עד היום 24 טבלאות ואפס שורות — אף שאילתה לא רצה מעולם. נבדקו תשע בדיקות שליליות, כולן נחסמו כמצופה, ויומן הביקורת רשם נכון שינוי בהזדהות. **29 טבלאות, כולן RLS, אפס עמודות `updated_at` בלי טריגר.**
- שלוש החלטות עקרוניות תועדו כ-ADR: `0006` (מתי lookup ומתי CHECK — הכלל: מי מכתיב את הרשימה, המשרד או המציאות הטכנית), `0007` (restrict כברירת מחדל + מחיקה רכה; התגלה שההגנה מפני מחיקת לקוח הייתה **תופעת לוואי מקרית** של ה-FK מיומן הביקורת, והייתה נעלמת בשקט עם המרתו לגנרי), `0008` (איחוד הערוצים; מבטל החלטה קודמת להשאיר שתי טבלאות).
- **נרשם כחוב מכוון:** הפרדת `hourly_cost` ל-`office_employee_compensation` היא הפתרון המועדף להרשאות, ותבוצע עם Auth. מודל 10-התפקידים×שדה מהגיליון הוא מנגנון אוריגמי; מה שמחייב הוא העיקרון בלבד — קו הפרדה אחד בין "פיננסי-רגיש" לכל השאר.
- **נשאר פתוח:** הכרעת הצפנת הסיסמאות (העמודה נעולה ל-NULL בינתיים), והעתקת מסמך מטרות-העל ל-`source-materials/` (יצחק מיישם).
- **נוצרה `supabase-selfhost/seeds/`** — הפרדה בין סכימה (migrations) לנתוני ייחוס (seeds). `0001_lookups.sql` זרע את כל 11 טבלאות ה-lookup, idempotent.
- **ממצא מהותי בזריעה:** הערכים הקונקרטיים ל-lookups **אינם קיימים בשום חומר מקור** — גיליון האיפיון מגדיר אותם כ"קישור לישות", כלומר השורות חיו באוריגמי ולא במפרט. תשע רשימות שוחזרו ממה שכבר מתועד ב-`docs/entities/` ומגיליון "רשימות" (קבוצות משתמשים); **שתיים — התמחויות וייעודי חשבון בנק — הן הצעה שלי ומסומנות ככאלה בקובץ**, וכך גם תקרת המחזור לעוסק פטור שהושארה ריקה במכוון (מספר משפטי שגוי גרוע מריק).
- **קבוצה ב': סדר תוקן בפעם השלישית, לפי הערת יצחק** — "בלי שירות, על מה יש עסקה?". המסמך מוכיח זאת: אופן התמחור בקטלוג קובע מה העסקה חייבת לשמור. אותה טעות כמו בלוח השנה הרגולטורי ובמכסה — צומת לפני שהצדדים מוגדרים.
- **הערת עבודה חשובה מיצחק:** נתתי משקל יתר לגיליון ולמסמכי האיפיון ופחות לתפקיד היועץ. האיפיונים הם **מסגרת ורעיון, לא סמכות עליונה** — הם נכתבו עבור אוריגמי, וחלק ניכר מהם הוא מנגנון של אותה פלטפורמה. התפקיד הוא לראות את המסגרת ולהפעיל שיקול דעת הנדסי. שונה גישת העבודה בהתאם, והנוסח ב-`status.md` עודכן.
- **אופיינו עשר ישויות כמהלך אחד** — קטלוג שירותים ותבניות חובה יחד, כי הם שני חצאים של אותו עיצוב. התובנה המרכזית: **הקטלוג הוא מתכון ולא מחירון** — הקטלוג מגדיר, העסקה מנביטה. שרשרת ההסקה: לקוח → עסקה → שירותים → תבניות → סינון לפי סוג ישות → חובות.
- **חמש סטיות מודעות מהאיפיון**, כולן מנומקות בקבצים: תדירות חיוב הופרדה מתדירות דיווח; "אופן תמחור" ירד; אחוזים ירדו (אינו נהוג); מדרגות מחיר נוספו (נהוג ולא היה); רשות הפכה לטבלה.
- **תשע הכרעות הוצגו לאישור, כולן נסגרו.** הבולטות: הנחות במנגנון אחד עם היקף אופציונלי ו**שדה חובה לפתרון קונפליקט** — יצחק דחה בצדק כלל גורף של "הספציפי גובר", כי הוא מחליף החלטה עסקית בהנחה טכנית. ושדה **סוג המבחין הנחה מהטבה** — הצעתי לסמן בהערות, ותוקן: טקסט חופשי אינו ניתן לשאילתה, וזו בדיוק הבעיה שהמרנו בגללה את `tags.category` ל-lookup.
- **"מתקן" אינו סוג יחס חמישי — תיקון עצמי.** הצעתי אותו כערך חמישי ליחסי הגומלין, וטעיתי ברמה: ארבעת היחסים מתארים קשר **בין תבניות** שנכון לנצח; דוח מתקן הוא **אירוע בין שני מופעים**. פוצל: `allows_amendments` על התבנית, הפניה עצמית על המופע. **ומכאן אזהרה לישות 10** — הנוסחה "הגשה = תבנית × תקופה × לקוח", אם תיהפך לאילוץ ייחוד, חוסמת הגשה מתקנת לחלוטין.
- **הוכרעה שאלת רשימת ההיתר מול רשימת הסינון** — שתי התשובות היו גרועות כי שתיהן נותנות לתבנית שנשכחה ליפול לצד כלשהו בשקט. הפתרון מבטל את השאלה: שדה `applies_to_all_entity_types` **`not null` בלי ברירת מחדל**, ואילוץ **נדחה** (`DEFERRABLE INITIALLY DEFERRED`) שמוודא ב-COMMIT שמי שסימן "לא לכולם" אכן מילא שורות. הדחייה הכרחית, אחרת סדר ההכנסות היה יוצר מצב בלתי אפשרי.
- **נרשמו כפריטים פתוחים מוגדרים:** עדיפות כתת-מערכת שלמה (קלטים: תגיות, קדימות חוקית, חומרת איחור, גודל קנס — סביר שמחושבת ולא מאוחסנת), מנגנון כיול שעות תקן מול נתוני אמת, והכיוון של הגשות מול משימות (שתי טבלאות + View).
- **ביקורת שלמות תיעוד לבקשת יצחק, ושני פערים אמיתיים נמצאו סביב ה-VIEW.** הוויו `active_clients` היה מוזכר בארבעה מקומות אבל: (א) הנימוק *למה* `security_invoker` קריטי — שבלעדיו הוויו הוא דלת עוקפת סביב RLS — נרשם כעובדה בלי הסבר; (ב) **מלכודת שלא תועדה בכלל:** Postgres מרחיב ומקפיא את `select *` ברגע יצירת הוויו, ולכן עמודה שתתווסף ל-`clients` לא תופיע בו מעצמה, בלי שגיאה. נכתב [`entities/active_clients.md`](entities/active_clients.md) ייעודי (חריגה מודעת מהכלל "קובץ לכל טבלה" — זהו VIEW).
- **`decisions/0005` הורחב בצ'קליסט מחייב** לכל מיגרציה מבנית: RLS → טריגר `updated_at` → רענון הוויו אם נגעת ב-`clients` → `security_invoker` לכל view → `restrict` כברירת מחדל → כלל lookup/CHECK. נכתב מפני שהתובנה על RLS תועדה ב-28/08 אבל **התיקון עצמו לא נכתב חזרה לקוד** — וזה בדיוק המצב שבו טעות חוזרת.
- **ממצא 18 נסגר באמת:** נכתב `supabase-selfhost/scripts/verify.sh` — עשר בדיקות (RLS, טריגרים, security_invoker, סנכרון הוויו, אובייקטים מול קבצי entities, מיגרציות/seeds/ADRs באינדקס, קישורים שבורים), מחזיר exit code וניתן לחיבור ל-CI. **נבדק שהוא תופס כשלים ולא רק מאשר** — הושתלו שני כשלים, הוא זיהה את שניהם והחזיר 1. ממצאים 1 ו-7 של הביקורת היו נתפסים על ידו ביום שנוצרו.
- **נתוני התרחישים נשמרו ב-DB** בהחלטת יצחק — סביבת פיתוח, לא פרודקשן. נכתב `9002_cleanup_scenario_data.sql` שמוחק בדיוק אותם לפי מזהים מקובעים, לקראת טעינת לקוחות אמיתיים; נבדק בהרצה עם rollback. תוקן בו באג לפני קיבוע: תנאי מחיקת שורות הביקורת היה מנוסח כ"כל הזדהות שכבר לא קיימת" והיה יכול למחוק היסטוריית ביקורת אמיתית.
- **הסכימה הופעלה מקצה לקצה בחמישה תרחישי לקוח** (`9001_scenario_smoke_test.sql`, בטרנזקציה עם rollback). שמונה אימותים עברו. השניים המשמעותיים: **ערוץ אחד שהוא רשמי לשני לקוחות** — הטענה שעליה נבנה `client_official_channels` הוכחה בפועל; ו**הזדהות שפג תוקפה מזוהה בשאילתה אחת** — השדה שנוסף היום מהאיפיון המקורי. כן אומתו: שני רו"ח באותו תפקיד עם היסטוריה (אינדקס חלקי), לקוח בהקמה בלי ח.פ., חסימת הפעלה בלי ח.פ., וחסימת מחיקת לקוח.

## 2026-08-30

- **התקבל מסמך "הנחיות עבודה" מלא ופורמלי מיצחק** — נשמר בזיכרון כ-`feedback_working_instructions` (מחליף שני קבצי פידבק קודמים). כולל: מתי חובה לעצור לפני ביצוע (מודל נתונים/מיגרציה/ארכיטקטורה/תלות חדשה/>3 קבצים/כסף-מסמכי-חובה-נתוני-לקוח/מחיקה), מבנה עצירה קבוע (מה/למה/מה נדחה/סיכון/מה נדרש), ליווי יזום (4.1-4.5), תיקון טעויות מיידי, הצגת חלופות עם המלצה, הבחנה בין "יודע/מניח/צריך לבדוק".
- **התחלת קבוצה ב' (שירותים וחובות).** הוחלט לא להיצמד לסדר הקבוצות של המסמך המקורי אלא לרצף תלויות אמיתי. נבנתה `periods` (תקופה, ישות 29) — **באופן מלא**, לא שלד: הוסבר מדוע היא נחוצה גם ב-SQL (לא רק פתרון למגבלת-רענון של אוריגמי) — עוגן לדריסת תאריך-יעד עתידית (לוח שנה רגולטורי) ולדה-דופליקציה של חומר תקופתי (ישות 12, עתידי) חוצה-תבניות. גלובלית לפי תדירות, לא פר-לקוח.
- זוהתה טעות סדר: "לוח שנה רגולטורי" (30) הוא צומת בין **תבנית חובה** ל"תקופה" — לא ניתן לבנות אותו לפני תבנית חובה עצמה. סדר מתוקן: קטלוג שירותים → מכסה תקופתית → עסקה → הנחה → תבנית חובה → לוח שנה רגולטורי → חובה תקופתית.
- `services` (קטלוג שירותים, ישות 5) נבנה **כשלד מינימלי בלבד** לפי בקשת יצחק — רק name/description/is_active, בלי סוג אספקה/אופן תמחור/תעריפים. יועמק בסבב נפרד אחרי שנעבור על שאר קבוצה ב', כמו שנעשה ל-`entity_types`/`tags` בקבוצה א'.
- הורץ בהצלחה (`20260830000000_periods_and_services_skeleton.sql`), RLS אומת על שתי הטבלאות.
- נפתחה ולא נסגרה: `client_service_quotas` (מכסה תקופתית, ישות 6) — זוהתה תלות בישות `רישום עבודה` (23, קבוצה ו', לא קיימת) לשדות "נוצל/יתרה/אחוז ניצול/דגל חריגה" (אלה View מחושב, לא עמודות מאוחסנות). ממתין להחלטת יצחק: לבנות רק חצי-הקצאה עכשיו, או לדלג על הישות כליל עד קבוצה ו'.

## 2026-08-26

- קריאת מסמכי האפיון (md, xlsx) והשיחה המשותפת מ-claude.ai — הבנת מודל 33 הישויות וההחלטות הארכיטקטוניות.
- הוקם git repo ב-`C:\Users\user\claude` (קומיט ראשון: `.gitignore`).
- הוסר Baserow (+DB+Redis) ואמולטור Android מה-VPS — פינוי ~3GB RAM (ר' `decisions/0001`).
- נוצר מפתח SSH ייעודי (`~/.ssh/greenwald_vps`) עם גישת root לשרת, בשליטת יצחק (מוסף/מוסר מ-`authorized_keys` לפי הצורך).
- דומיין `y-h-m.com` (Hostinger) הועבר ל-Cloudflare (nameservers), רשומת A `supabase` → IP השרת.
- Supabase self-hosted הותקן והורם דרך docker-compose רשמי ב-`/opt/supabase/docker` (ר' `decisions/0001`).
- אותרה ותוקנה בעיית ניתוב Traefik↔Envoy (502 Bad Gateway) — עבר לניתוב פנימי דרך רשת Docker משותפת (ר' `decisions/0002`).
- אומת end-to-end: `https://supabase.y-h-m.com` עובד עם HTTPS תקין.
- הוקשח SSH (מפתחות בלבד), הותקנו ufw ו-fail2ban. פורט 3000 (Easypanel) נשאר חשוף במכוון (ר' `decisions/0003`).
- הוקם bucket R2 (`greenwald-db-backups`) וטוקן API מוגבל, נבנה סקריפט גיבוי יומי (`pg_dumpall` → gzip → R2), מתוזמן ב-cron (ר' `decisions/0004`).
- הוקמה מערכת התיעוד הזו (`docs/`).
- `overview.md` הועשר עם התוכן המלא ממסמך "מטרות CRM" (Google Docs): כוכב צפוני, תיחום scope מפורש, עשר המטרות עם יעדים מדידים, ושלוש הבהרות (מטרות = עדשה חוצה-שלבים לא שיוך לשלב; אין פורטל לקוח בשלב זה; אמינות מנוע התזכורות כשיקול מפורש).
- נכתב איפיון מפורט לישות לקוח (`docs/entities/client.md`) — תרגום מלוגיקת אוריגמי ל-SQL, לא קוד. כולל 3 שינויי עיצוב מכוונים (client_status כטבלת lookup עם דגל התנהגות במקום enum; audit log חדש כמענה למטרה 9; ביטול שדות טלפון/מייל "ראשיים" קבועים לטובת טבלת ערוצי-קשר). **עדיין אין קוד SQL בפועל.**
- פוצל: ארבע ישויות התלות (`entity_types`, `reporting_frequencies`, `office_employees`, `tags`) הוצאו מ-`client.md` לקבצים נפרדים משלהן (כשלד מינימלי) — כל ישות במקום שלה, גם אם עדיין לא באיפיון מלא.
- **תיקון מבנה נוסף** לפי בקשה מפורשת: `client.md` נמחק לגמרי (לא היה אמור להיות "מסמך-על"). **כל טבלת SQL קיבלה קובץ נפרד משלה** (9 טבלאות: clients, client_statuses, client_contact_channels, client_bank_accounts, client_professional_credentials, client_financial_credentials, client_notes, client_tags, clients_audit_log) — בלי היררכיה, כל קובץ שווה-מעמד. גם נוספה **עמודת תרגום-שם** (עברית) לכל שדה בכל הקבצים, כולל ארבעת השלדים הקיימים — לא רק הסבר תפקיד.
- נוספה לכל 13 קבצי הישויות פסקת פתיחה אחידה "מה זה ולמה זה משרת" — תיאור ראשי של הטבלה, לא רק מילון שדות.
- נכתב קוד SQL מלא (13 טבלאות) לישות הלקוח, לפי מה שכבר תועד — `supabase-selfhost/migrations/20260828000000_client_entity.sql`. כולל טריגר `log_client_changes` שמזין אוטומטית את `clients_audit_log`.
- **המיגרציה הורצה בהצלחה על ה-DB החי** (דרך SQL Editor ב-Studio, לא SSH — יצחק העדיף כך). נבחר "Run and enable RLS" — ר' `decisions/0005`. כל 13 הטבלאות קיימות בפועל עכשיו.
- **קבוצה א' נסגרה במלואה.** אחרי אתגור עצמי מפורש (יצחק ביקש שלא רק אקבל אלא גם אתגר): `entity_types` קיבל `turnover_threshold` — סוגר מעגל עם `client_turnover_history` (מטרה 8, זיהוי לקוח שחצה סף). `tags.category` (טקסט חופשי) הוחלף ב-`tag_categories` (lookup) — כי המסמך המקורי אומר "כל קטגוריה מפעילה לוגיקה שונה", אותו סימן כמו `client_status`. `client_tags` קיבל שדות ביקורת (מי/מתי הצמיד, אוטומטי/ידני). `reporting_frequencies` נבדק שוב ונשאר כמו שהוא — האזהרה המקורית עדיין תקפה. הורץ בהצלחה (`20260828060000`), RLS נבדק. **נשמרה גם הנחיה קבועה בזיכרון:** לאתגר גם רעיונות שלי וגם בקשות של יצחק, לא רק להסכים.
- הוער פער ידוע ונדחה בכוונה: "נוכחות" (ישות 24, כניסה/יציאה יומיומית) — שונה מ-`employee_absences`, נדחה לקבוצה ו' יחד עם "רישום עבודה" (ישות 23) שעדיין לא קיימת.
- **`office_employees` עבר תיקון ארכיטקטוני נוסף, אחרי חפירה עמוקה יותר לפי כל עשר המטרות** (לא רק 5-6): פרטים אישיים (שם/ת.ז./פרטי קשר) הועברו ל-`contacts` דרך `contact_id` — במקום כפילות מבנה עם מה שכבר בנינו לאנשי קשר. נוספו תנאי העסקה אמיתיים (`employment_type`, `employment_percentage`, `weekly_capacity_hours`) — משרתים גם מטרה 6 (רווחיות) וגם מטרה 9 (הגנה משפטית על תנאי העסקה). נוספה טבלת `employee_absences` (היעדרויות **זמניות**, לא עזיבה קבועה) — עם `covering_employee_id` פר-היעדרות, **במפורש לא אוטומטי**: אין "מחליף קבוע" סטטי על העובד, מנהל מחליט לכל מקרה. הורץ בהצלחה (`20260828050000`), RLS נבדק על שתי הטבלאות.
- **`office_employees` עבר איפיון מלא** (היה שלד): פוצל `full_name`→`first_name`/`last_name` (עקבי עם `contacts`), נוספו `id_number` (PII), `phone`/`email` (שדות בודדים, לא טבלת ערוצים — עובד לא צריך את מורכבות ה-Omnichannel), `hire_date`, `is_active`+`departure_date`+`departure_reason` (עובד שעזב מסומן לא-פעיל, לא נמחק). הורץ בהצלחה (`20260828040000`), RLS נבדק ונשאר פעיל.
- נסגרה שאלה פתוחה שנשארה תלויה: `client_contacts` קיבל שדות היסטוריה (`position_at_business`, `responsible_since`, `ended_at`, `is_active`) — רלוונטי לכל תפקיד, לא רק `primary`. אילוץ הייחודיות הוחלף מ-UNIQUE(client_id,contact_id,role) לאינדקס חלקי — רק אדם *פעיל* אחד לתפקיד, בלי לחסום היסטוריה. הורץ בהצלחה על ה-DB (`20260828030000`). גם נוקתה הערה מיושנת ב-`clients.md` על מחזור מע"מ (כבר נפתר קודם).
- נוסף `docs/entities/INDEX.md` — אינדקס ייעודי לתיקיית entities/, טבלה עם תרגום+הסבר+סטטוס לכל 20 הטבלאות. `docs/INDEX.md` הראשי עודכן להפנות אליו במקום לשכפל את הרשימה. `CLAUDE.md` עודכן בהתאם (כלל קבוע).
- **כל 7 הטבלאות החדשות + 3 העדכונים (ארכיטקטורת אנשי קשר, מחזור, תפקידים) הורצו בהצלחה על ה-DB החי** (`20260828020000_contacts_architecture.sql`, דרך SSH). התגלתה ותוקנה בעיה: הרצה דרך SSH לא מפעילה RLS אוטומטית (בניגוד ל-Studio UI) — 7 הטבלאות נוצרו רגע בלי RLS, תוקן ידנית מיד, וקובץ ה-migration עודכן להבא. פירוט מלא ב-`decisions/0005` (תוספת).
- מחזור מע"מ/צפוי: יצחק בחר טבלה היסטורית (לא עמודה בודדת) — נוספה `client_turnover_history` (שורה לכל לקוח+שנה, `actual_turnover`+`expected_turnover`).
- נוספה `employee_roles` (ישות 27 מהמודל המקורי, נפספסה קודם) — lookup פתוח, `office_employees.role` (טקסט חופשי) הוחלף ב-`role_id` (FK). `client_notes` קיבלה `relevant_role_id` (תוקף לתפקיד, בנוסף לתוקף בזמן). `clients` קיבלה `general_notes` (חופשי, בלי תוקף) — לצד `client_notes`, לא תחליף.
- כתובת (רחוב/עיר/מיקוד) נוספה גם ל-`contacts` — תכונה של האדם, לא של הקשר לתפקיד. `client_contact_channels` **נשאר** לצד `contact_channels` — ערוץ מוסדי (עסק) שונה במהות מערוץ אישי (אדם), גם אם אותו אדם עונה בשניהם בפועל.
- **תוקנה טעות ארכיטקטונית:** שדות "חותם" (`signatory_*`) שישבו ישירות על `clients` הועברו לארכיטקטורת אנשי קשר מלאה — 5 טבלאות חדשות מאופיינות: `contacts` (ישות 2 מהמודל המקורי, סוף-סוף נבנתה), `contact_channels`, `client_contacts` (קישור עם `role`), `responsibility_areas` (lookup פתוח, כמו `tags`), `client_contact_responsibilities`. שדות ספציפיים-לתפקיד (כמו "אחראי ראשי") יושבים על `client_contacts`, לא על `contacts` ולא על `clients`. **עדיין לא נכתב קוד SQL לזה — רק איפיון.**
- `clients.md` עודכן: הוסרו שדות signatory, נוספו 12 שדות חדשים (תיק מס הכנסה/ניכויים/ביטוח לאומי, תחנת מע"מ, פקיד שומה, אתר, תיאור עסק, תת-תחום, תאריך פתיחה, קוד סיווג, תאריך תחילת התקשרות, סיבת עזיבה) לפי בקשה מפורטת. שדות "וותק"/"הערה בתוקף" נדחו במכוון — נגזרים, לא עמודות.
- שדה `address` פוצל לשדות מובנים (`street_address`, `city`, `postal_code`) לפי בקשה — `20260828010000_client_structured_address.sql`, הורץ בהצלחה על ה-DB החי דרך SSH. `clients.md` עודכן בהתאם.
- **ביקורת שלמות תיעוד** (לקראת מבחן: העברת התיקייה לשיחת Claude Code חדשה, ללא הזיכרון הפנימי שלי). נמצאו ותוקנו 3 פערים: (1) כלל "סגנון עבודה" (יועץ, מסביר, לא מנחש) הועבר מהזיכרון הפנימי אל `CLAUDE.md` — עכשיו נוסע עם התיקייה. (2) שני מסמכי המקור הועתקו בפועל לתוך `docs/source-materials/` (לא רק הפניה ל-Downloads חיצוני). (3) קישורי ה-URL המקוריים (Google Docs, claude.ai) נוספו בטקסט ב-`overview.md`. גם נוסף כלל ל-`CLAUDE.md` לגבי מבנה קבצי entities (טבלה נפרדת לכל SQL table, 5 עמודות קבועות) כדי שיישמר עקבי בהמשך.
