# סטטוס נוכחי

**עדכון אחרון:** 2026-08-31

## מה הושלם

- תשתית שרת מוכנה: VPS נוקה, DNS/Cloudflare, Supabase self-hosted רץ ותקין, נגיש דרך `https://supabase.y-h-m.com` עם HTTPS תקין (נבדק end-to-end).
- אבטחה בסיסית: SSH מוקשח, fail2ban, Postgres/API חסומים מהאינטרנט (מאומת חיצונית).
- גיבויים אוטומטיים ל-R2 פועלים (cron יומי).
- Git repo מאותחל, סדרת קומיטים בוצעה, כולל כל ה-migrations שהורצו.
- מערכת התיעוד (docs/) פעילה, עברה ביקורת שלמות.
- **קבוצה א' (ליבה) סגורה במלואה.**
- **קבוצה ב' (שירותים וחובות) התחילה** — `periods` מלאה, `services` שלד מינימלי בכוונה. הסדר תוקן: לוח שנה רגולטורי (30) אחרי תבנית חובה (9), לא לפניה.
- **29 טבלאות ב-DB החי, כולן עם RLS מופעל** (בלי policies עדיין — חסום כברירת מחדל ל-anon/authenticated). ר' [`entities/INDEX.md`](entities/INDEX.md) לרשימה המלאה.

## ביקורת ארכיטקטונית (31/08/2026) — 18 ממצאים, 14 נסגרו

ביקורת מלאה של המצב הקיים מול ה-DB החי, קבצי המיגרציה וחומרי המקור. אותרו 5 ממצאים חמורים, 10 בינוניים ו-3 קלים.

**מה תוקן במיגרציה `20260831000000_audit_remediation.sql`:**

| # | הממצא | מה נעשה |
|---|---|---|
| 1 | המיגרציות לא שחזרו את ה-DB — RLS חסר על 13 טבלאות בקוד | RLS הופעל בקוד; ה-DB והקבצים תואמים |
| 2 | `password_encrypted` הבטיח הצפנה שלא קיימת | שם שונה ל-`password_pending_encryption`, **נעול ל-NULL** |
| 3 | ההזדהויות — הרגישות ביותר — לא בוקרו כלל | `audit_log` גנרי, מוחל על `clients` ועל `client_credentials` |
| 5 | שדות מהאיפיון המקורי שלא מומשו | הושלמו: חשבונות בנק, הזדהויות, ערוצים |
| 7 | `updated_at` בלי טריגר ב-`periods`/`services` | טריגרים נוספו; אפס עמודות `updated_at` בלי טריגר |
| 8 | שתי טבלאות ערוצים ללא ייחוד משותף | אוחדו ל-`channels` + `client_official_channels` — `decisions/0008` |
| 9 | `cascade` בכל מקום, בלי מחיקה רכה | `restrict` + `deleted_at` + View `active_clients` — `decisions/0007` |
| 10 | `CHECK` מול lookup בלי כלל | כלל נקבע ויושם — `decisions/0006` |
| 11 | חסרו אילוצי תקינות על תאריכים | `CHECK` על `periods`, היעדרויות, קשרי לקוח, שנת מחזור |
| 13 | `legal_id_number NOT NULL` חסם לקוח בהקמה | nullable + טריגר שאוכף רק במעבר ל"פעיל" |
| 14 | `contact_id` nullable וללא ייחוד | `not null unique` |
| 15 | `specializations text[]` | הומר ל-lookup + M2M |
| 16 | תיאור שגוי של חומר המקור ב-INDEX | תוקן, כולל הגדרת מעמד המסמך |

**נבדק תפקודית:** תשע בדיקות שליליות (לקוח פעיל בלי ח.פ., ערוץ עם שני בעלים/בלי בעלים, טלפון כפול, שני ערוצים רשמיים, סוג לא תואם, סיסמה גלויה, מחיקת לקוח עם תוכן, תקופה הפוכה) — כולן נחסמו כמצופה. יומן הביקורת רשם נכון שינוי בהזדהות.

## מה לא הושלם / פתוח

**⚠️ החלטות פתוחות שדורשות יצחק:**

- **הצפנת סיסמאות (ממצא 2).** העמודה נעולה ל-NULL עד להכרעה. היעד שנבחר: הוצאה למנהל סיסמאות חיצוני, עם הפניה בלבד ב-DB. **לא לפתוח את הנעילה** לפני יישום ותיעוד ב-ADR.
- **מסמך מטרות-העל (ממצא 17)** — מקושר מה-xlsx, טרם הועתק ל-`source-materials/` כפי שמחייב הכלל ב-`CLAUDE.md`. יצחק ייישם.

**חוב שנרשם במכוון ולא בוצע:**

- **הפרדת `hourly_cost` לטבלה נפרדת (ממצא 6)** — **זהו הפתרון המועדף** ל"עלות שעה/רווחיות/גבייה — בעלים ומנהל משרד בלבד": `office_employee_compensation` נפרדת + policy אחת ברמת טבלה, במקום הרשאות ברמת עמודה שמסורבלות ב-Postgres. יבוצע כשתיבנה מערכת Auth. מתועד גם ב-[`entities/office_employees.md`](entities/office_employees.md). **ככל שנבנה מעל הטבלה, כך זה מתייקר.**
- **ישות 11 (לוח דיווח פעיל) — ההחלטה הוקדמה (ממצא 12).** תוכרע מיד אחרי תבנית חובה (9) ולפני חובה תקופתית (10), לא בסוף הקבוצה. הסיבה: שתי עמודות התדירות על `clients` מקדימות את ההחלטה, וקבוצה ב' תתנגש בהן.
- **`audit_log` תופס עדכון בלבד** — יצירה ומחיקה אינן מתועדות, וצפייה אינה ניתנת לתפיסה בטריגר. ל-`client_credentials` זה פער אמיתי.
- **`changed_by` לא ממולא** — תלוי ב-Auth שעדיין לא קיים.
- ~~**הצלבת תיעוד↔DB (ממצא 18)**~~ — **נסגר 31/08/2026.** נכתב `supabase-selfhost/scripts/verify.sh` עם עשר בדיקות, מחזיר exit code. ממצאים 1 ו-7 של הביקורת (13 טבלאות בלי RLS בקוד, שתי עמודות `updated_at` מתות) היו שניהם נתפסים על ידו ביום שנוצרו. נבדק שהוא אכן תופס כשלים, לא רק מאשר. **להריץ בסוף כל קבוצה ולפני כל קומיט שנוגע בסכימה.**

**פערי תשתית ידועים, לא דחופים:**

- פורט 3000 (Easypanel) עדיין חשוף לאינטרנט.
- RLS policies בפועל לא נכתבו — הכל חסום כברירת מחדל (מכוון, ר' `decisions/0005`).
- `office_employees.auth_user_id` — יתווסף רק כשתיבנה מערכת Auth.
- סיסמת ה-Dashboard לא הוחלפה (יצחק ביקש לא לגעת).
- הצפנת PII (`legal_id_number`, `contacts.id_number`) — שאלה כללית פתוחה, לא חוסמת.

## איך קוראים את גיליון האיפיון (`מנהל_גיליונות1.xlsx`)

הגיליון הוא נכס מרכזי ונדרש לאיפיון — **ובו בזמן נכתב עבור אוריגמי**, ולכן חלק ממנו הוא מנגנון של אותה פלטפורמה ולא מידע. שלוש מחלקות שדה:

1. **שדות מידע אמיתיים** → הופכים לעמודות. (ייעוד חשבון בנק, תוקף הזדהות, תיאור ערוץ.)
2. **שדות נוסחה** — "וותק", "שם מלא", "בתוקף?", "סיכום הערות מקצועיות", "פעילות אחרונה" → **View או חישוב בשאילתה, לא עמודה.** האיפיון עצמו מזהיר ש"נוסחאות באוריגמי לא מתרעננות מעצמן"; ב-Postgres הבעיה לא קיימת ולכן גם השדה מיותר.
3. **שדות קישור הפוך** — "עסקאות", "הגשות", "חיובים", "מסמכים", "מטלות" → **לא עמודות כלל.** באוריגמי חובה להצהיר על שני כיווני הקשר; ב-SQL ה-FK בטבלת הילד מספיק.

לכן "82 שדות בגיליון לקוחות" אינו פער של 82 עמודות. **הנוהל:** לפני איפיון כל ישות — לפתוח את הגיליון המקביל ולעבור על השדות, ולסווג כל אחד לפי שלוש המחלקות. הסיכום המזוקק משמש להבנת הכוונה וההחלטות; הגיליון משמש למלאי השדות; **המבנה בפועל הוא שיקול דעת הנדסי ולא העתקה.**

## זריעה ובדיקת תרחישים — בוצע 31/08/2026

**11 טבלאות ה-lookup נזרעו** (`seeds/0001_lookups.sql`, idempotent). בלעדיהן לא ניתן היה להזין לקוח בכלל, כי `status_id` ו-`entity_type_id` הם `NOT NULL`.

**חשוב — מקור הערכים.** הערכים הקונקרטיים **אינם קיימים בחומרי המקור**: גיליון האיפיון מגדיר את השדות האלה כ"קישור לישות", כלומר השורות עצמן חיו באוריגמי ולא במפרט. לכן כל רשימה בקובץ הזריעה מסומנת במקורה, ושתיים מהן דורשות אישור:

| טבלה | מקור | סטטוס |
|---|---|---|
| entity_types, client_statuses, reporting_frequencies, responsibility_areas, client_contact_roles, tag_categories, credential_types, absence_types | מתועד ב-`docs/entities/` | ✅ |
| employee_roles | גיליון "רשימות", עמודת קבוצות משתמשים | ✅ (הוחרגו "לקוח (פורטל)" ו"מערכת/אוטומציה" — אלה סוגי שחקן ב-Auth, לא עובדי משרד) |
| **specializations** | לא מעוגן בשום מקור | ⚠️ **הצעה שלי — לאישורך** |
| **bank_account_purposes** | לא מעוגן בשום מקור | ⚠️ **הצעה שלי — לאישורך** |
| **entity_types.turnover_threshold** | לא מעוגן | ⚠️ **הושאר ריק בכוונה** — תקרת עוסק פטור משתנה משנה לשנה, ומספר משפטי שגוי גרוע מריק |

**חמישה תרחישים הורצו מקצה לקצה** (`seeds/9001_scenario_smoke_test.sql`, בטרנזקציה עם rollback — לא נזרע לצמיתות). שמונה אימותים, כולם עברו:

1. שישה לקוחות ב-5 תרחישים, כולל אחד בהקמה **בלי ח.פ.** — מצב שהיה בלתי אפשרי אתמול.
2. **ערוץ אחד שהוא רשמי לשני לקוחות** (בעלים עם שתי חברות וקו משותף) — הטענה שעליה נבנה מבנה `client_official_channels`, מוכחת ולא משוערת.
3. שני רו"ח מבקרים באותו תפקיד — אחד שהסתיים ואחד פעיל. האינדקס החלקי מאפשר היסטוריה.
4. **הזדהות שפג תוקפה מזוהה בשאילתה אחת** — השדה שנוסף היום מהאיפיון המקורי.
5. חשבונות בנק עם ייעוד (גבייה מול החזרים).
6. ה-View `active_clients` מחזיר נכון.
7. ניסיון להעביר לקוח בלי ח.פ. לסטטוס "פעיל" — **נחסם** ע"י הטריגר.
8. ניסיון מחיקת לקוח שעזב — **נחסם** ע"י `restrict`.

**מה זה כן הוכיח ומה לא.** זה הוכיח שהמודל מחזיק ושהאילוצים עובדים. זה **לא** מחליף טעינה של ספר הלקוחות האמיתי, שהוא מה שיחשוף הפתעות מהשטח — פורמטים אמיתיים של מספרי תיק, שדות שאיש לא מילא, כפילויות, ואורכי שדה בפועל.

### נתוני הבדיקה יושבים ב-DB (סביבת פיתוח)

בהחלטת יצחק — זו סביבת פיתוח ולא פרודקשן, ולכן ששת לקוחות התרחישים **נשמרו** ב-DB כדי שניתן יהיה לעיין בהם ב-Studio. המצב כרגע: 6 לקוחות, 8 אנשי קשר, 2 עובדי משרד, 2 ערוצים, 3 תיוגי ערוץ רשמי, 2 הזדהויות, 2 חשבונות בנק.

**לפני טעינת לקוחות אמיתיים — ובוודאי לפני פרודקשן — להריץ `seeds/9002_cleanup_scenario_data.sql`.** הוא מוחק בדיוק את שורות הבדיקה לפי מזהים מקובעים, ולא ייגע בלקוח אמיתי גם אם יישא שם דומה. נבדק בהרצה עם rollback. הסיבה שהוא נכתב עכשיו ולא אז: בעוד חודשיים כבר לא יהיה ברור אילו שורות היו בדיקה.

**הערה על Studio:** RLS מופעל בלי policies, אבל Studio ניגש עם `service_role` שעוקף RLS — ולכן הנתונים נראים שם. דרך ה-API עם `anon key` הם לא ייראו, וזה מכוון.

## הצעד הבא

1. **אישור שתי רשימות ההצעה** (התמחויות, ייעודי חשבון בנק) ותקרת המחזור לעוסק פטור.
2. **טעינת לקוחות אמיתיים** — לפחות חמישה מהספר של המשרד.
3. ואז המשך קבוצה ב' לפי הסדר: עסקה (7) → הנחה (8) → תבנית חובה (9) → **החלטה על ישות 11** → לוח שנה רגולטורי (30) → חובה תקופתית (10) → סבב העמקה ל-`services` ול-`client_service_quotas` (6). האיפיון ייעשה **מתוך הגיליונות** ולא מהסיכום — ר' "איך קוראים את גיליון האיפיון" למעלה.
